#include <bits/stdc++.h>
using namespace std;

int removeDuplicates(int arr[], int n) {
  int k;
  return k;
}

int main() {
  int arr[] = {1, 4, 4, 3, 1, 2, 2, 2, 3, 3};
  int n = sizeof(arr) / sizeof(arr[0]);

  int h = removeDuplicates(arr, n);

  return 0;
}
